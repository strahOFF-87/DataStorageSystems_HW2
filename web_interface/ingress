apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-interface-ingress
spec:
  rules:
  - host: your-domain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-interface
            port:
              number: 80
